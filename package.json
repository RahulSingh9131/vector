{
  "name": "vector",
  "version": "1.0.0",
  "description": "",
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "bun@1.3.4",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "type-check": "turbo run type-check",
    "clean": "turbo run clean",
    "format": "turbo run format",
    "turbo:clean": "rm -rf .turbo node_modules"
  },
  "devDependencies": {
    "turbo": "^2.6.3",
    "typescript": "^5.9.3"
  }
}
